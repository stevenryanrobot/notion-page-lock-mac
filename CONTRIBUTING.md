# 参与开发

欢迎提交可复现的问题和小范围修复。此项目目前针对 macOS 上的 Notion 7.33.0，是本地视图遮挡插件。

## 本地检查

使用 Node.js 22.12 或更新版本：

```sh
npm ci
npm test
```

原生集成测试还需要 macOS 与 `/Applications/Notion.app`（7.33.0）：

```sh
npm run test:electron
```

测试在 `work/tests/` 生成独立的应用副本和数据目录，以合成页面验证，不需要登录真实工作区。测试会打开窗口并切换焦点，请在不操作其他窗口时运行。结束后结果位于对应测试目录的 `results/electron-results.json`。持续集成只运行核心逻辑和语法检查，不下载或发布 Notion 二进制。

## 提交范围

- 保留密码验证与网页渲染之间的进程隔离。
- 不允许测试开关、远程网页或开发者模式绕过密码验证进入产品代码。
- 明确区分本地遮挡和真正加密，不将搜索、AI、导出或其他客户端描述为受保护。
- 新适配的 Notion 版本必须检查程序结构并验证；不要取消构建时的版本检查。
- 不提交 Notion 原程序、解包后的文件、用户缓存、Cookie、密码或登录数据。
- 修改锁定和导航逻辑时，补充能够捕捉实际故障的测试。

自己的插件代码采用 MIT 许可证，Notion 及其他第三方文件保留各自的权利。
