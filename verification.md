# 验证记录

验证日期：2026-09-13。环境：Apple M2、macOS 15.7.9、本机 Notion 7.33.0 所带 Electron 43.6.0。

## 已通过

5 组核心测试、17 个桌面集成检查均通过。桌面集成测试运行在独立的应用 ID 与数据目录中，通过协议处理器加载合成页面，未读取或修改用户工作区内容。

| 检查                                         | 结果 |
| -------------------------------------------- | ---- |
| 首次设置独立密码后立即保护页面               | 通过 |
| 冷启动受保护页面时正文及子元素隐藏，交互关闭 | 通过 |
| 采样的首次加载动画帧均未显示正文             | 通过 |
| Notion 网页拿不到密码窗口的调用接口          | 通过 |
| 错误密码保持锁定                             | 通过 |
| 正确密码经本地独立窗口验证后显示页面         | 通过 |
| 解锁一页不解锁其他受保护页面                 | 通过 |
| 立即重新上锁隐藏已加载内容                   | 通过 |
| 普通页面正常显示                             | 通过 |
| 单页应用的 history 路由变化同步触发遮挡      | 通过 |
| 合成页面中的受保护块/预览触发遮挡            | 通过 |
| macOS 锁屏事件清除解锁状态                   | 通过 |
| 应用失去焦点后清除解锁状态                   | 通过 |
| 新窗口继承保护状态                           | 通过 |
| 重新上锁使之前打开的密码验证窗口失效         | 通过 |
| 验证密码后可以持久取消页面保护               | 通过 |
| 配置不可读时保持隐藏                         | 通过 |

核心测试另外覆盖了带标题/UUID/预览参数的页面 ID 解析、非 Notion 域名拒绝、密码长度、scrypt 校验、错误尝试限速、文件权限、重启后重新锁定、损坏文件不被覆盖、保存失败保持原保护状态及非法页面元数据处理。

已检查桌面副本的本机签名，开发过程中实际启动到 Notion 登录页，菜单栏出现“隐私锁”。最终重新构建的副本签名通过，内置插件文件与交付源码逐项一致，启动后进程保持运行且未产生新的崩溃报告；9 月 13 日上午解锁 Mac 后，已再次通过界面检查确认最终副本显示邮箱登录表单、“隐私锁”菜单中的锁定/解锁/重新上锁/管理入口正常显示；在登录页点击“锁定当前页面”正确提示先打开 Notion 页面，关闭提示后登录表单恢复。5 项核心测试再次通过。锁定界面预览来自合成测试页面的真实 Electron 渲染结果。

## 尚未验证

- 你的真实 Notion 登录会话、页面编辑与全部数据库/内嵌布局。
- Google、Apple、SSO 等第三方登录方式。
- 所有导航时序、系统切换动画、窗口缩略图及屏幕捕捉场景下的绝对零曝光。
- 递归子页面保护、搜索/AI/导出/同步块内容过滤。
- Intel Mac、其他 macOS 版本、Notion 更新后的兼容性。

“采样动画帧未显示正文”仅针对测试场景，不等于所有真实页面、所有系统动画均已证明零闪现。固定 5 分钟的重新上锁定时器已实现，集成测试未等待完整 5 分钟。

这是一款本地视图遮挡插件，不是密码学意义上的页面访问控制系统。它不改变 Notion 云端权限。
